includeConfigs:
  - ${CP_PIPE_PROCESSING_DIR}/config/bps/bps_default_configs.yaml

extraQgraphOptions: "--config isr:overscan.fitType='MEDIAN_PER_ROW' --config isr:doLinearize=False --config isr:doCrosstalk=False"

operator: jchiang
pipelineYaml: "${CP_PIPE_DIR}/pipelines/cpPtc.yaml"
instrument: lsst.obs.lsst.LsstCam

payload:
  ptc_run: 13144
  exposure_list: (3021120600576..3021120600666)  # Use first 60 for testing.
  b_protocol_run: 13162
  weekly: w_2022_34
  inCollection: u/{operator}/flat_0_{b_protocol_run}_{weekly}
  payloadName: ptc_0_{ptc_run}_{weekly}
  butlerConfig: ${BUTLER_CONFIG}
#  dataQuery: "instrument='LSSTCam' and exposure.science_program='{ptc_run}' and exposure.observation_type='flat' and exposure.observation_reason='flat' and exposure in {exposure_list}"
  dataQuery: "instrument='LSSTCam' and exposure.science_program='{ptc_run}' and exposure.observation_type='flat' and exposure.observation_reason='flat'"
